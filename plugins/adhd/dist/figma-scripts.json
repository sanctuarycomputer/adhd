{
  "extract": "\"use strict\";\n\n// src/core/hash.ts\nfunction stableStringify(v) {\n  if (v === null || typeof v !== \"object\") return JSON.stringify(v);\n  if (Array.isArray(v)) return \"[\" + v.map(stableStringify).join(\",\") + \"]\";\n  const keys = Object.keys(v).sort();\n  return \"{\" + keys.map((k) => JSON.stringify(k) + \":\" + stableStringify(v[k])).join(\",\") + \"}\";\n}\nfunction fnv32(s, seed) {\n  let h = seed >>> 0;\n  for (let i = 0; i < s.length; i++) {\n    h ^= s.charCodeAt(i);\n    h = Math.imul(h, 16777619) >>> 0;\n  }\n  return h >>> 0;\n}\nfunction fnv1a64(s) {\n  const a = fnv32(s, 2166136261);\n  const b = fnv32(s, 2166136261 ^ 1540483477);\n  return a.toString(16).padStart(8, \"0\") + b.toString(16).padStart(8, \"0\");\n}\n\n// src/figma/extract.ts\nvar DEFAULT_CHUNK_SIZE = 30;\nvar B64_CHARS = \"ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/\";\nfunction bytesToBase64(bytes) {\n  let out = \"\";\n  for (let i = 0; i < bytes.length; i += 3) {\n    const b0 = bytes[i];\n    const b1 = bytes[i + 1];\n    const b2 = bytes[i + 2];\n    out += B64_CHARS[b0 >> 2];\n    out += B64_CHARS[(b0 & 3) << 4 | (b1 ?? 0) >> 4];\n    out += b1 === void 0 ? \"=\" : B64_CHARS[(b1 & 15) << 2 | (b2 ?? 0) >> 6];\n    out += b2 === void 0 ? \"=\" : B64_CHARS[b2 & 63];\n  }\n  return out;\n}\nfunction base64ToBytes(b64) {\n  const clean = b64.replace(/=+$/, \"\");\n  const bytes = [];\n  let buffer = 0;\n  let bits = 0;\n  for (const ch of clean) {\n    const idx = B64_CHARS.indexOf(ch);\n    if (idx === -1) continue;\n    buffer = buffer << 6 | idx;\n    bits += 6;\n    if (bits >= 8) {\n      bits -= 8;\n      bytes.push(buffer >> bits & 255);\n    }\n  }\n  return bytes;\n}\nfunction encodeCursor(offset) {\n  const s = String(offset);\n  const bytes = [];\n  for (let i = 0; i < s.length; i++) bytes.push(s.charCodeAt(i));\n  return bytesToBase64(bytes);\n}\nfunction decodeCursor(cursor) {\n  const bytes = base64ToBytes(cursor);\n  let s = \"\";\n  for (const b of bytes) s += String.fromCharCode(b);\n  const n = Number(s);\n  if (!Number.isInteger(n) || n < 0) throw new Error(`extract: invalid cursor \"${cursor}\"`);\n  return n;\n}\nasync function serializeVariable(figma2, id) {\n  const v = await figma2.variables.getVariableByIdAsync(id);\n  if (!v) throw new Error(`extract: variable not found for id ${id}`);\n  return { id: v.id, name: v.name, resolvedType: v.resolvedType, valuesByMode: v.valuesByMode };\n}\nfunction serializeStyle(s) {\n  return { id: s.id, name: s.name, boundPrimitiveIds: s.boundPrimitiveIds ?? [] };\n}\nasync function readDocShape(figma2) {\n  const collections = await figma2.variables.getLocalVariableCollectionsAsync();\n  const collectionsMeta = collections.map((c) => ({\n    id: c.id,\n    name: c.name,\n    modes: c.modes,\n    variableIds: c.variableIds\n  }));\n  const flat = [];\n  collectionsMeta.forEach((c, collectionIndex) => {\n    for (const variableId of c.variableIds) flat.push({ collectionIndex, variableId });\n  });\n  return { collectionsMeta, flat };\n}\nasync function runExtract(figma2, args) {\n  const chunkSize = args.chunkSize ?? DEFAULT_CHUNK_SIZE;\n  const offset = args.cursor ? decodeCursor(args.cursor) : 0;\n  const { collectionsMeta, flat } = await readDocShape(figma2);\n  const slice = flat.slice(offset, offset + chunkSize);\n  const nextOffset = offset + slice.length;\n  const done = nextOffset >= flat.length;\n  const byCollection = /* @__PURE__ */ new Map();\n  for (const ref of slice) {\n    const serialized = await serializeVariable(figma2, ref.variableId);\n    const arr = byCollection.get(ref.collectionIndex) ?? [];\n    arr.push(serialized);\n    byCollection.set(ref.collectionIndex, arr);\n  }\n  const collectionsOut = [];\n  if (offset === 0) {\n    collectionsMeta.forEach((c, i) => {\n      collectionsOut.push({ id: c.id, name: c.name, modes: c.modes, variables: byCollection.get(i) ?? [] });\n    });\n  } else {\n    for (const [i, vars] of byCollection) {\n      const c = collectionsMeta[i];\n      collectionsOut.push({ id: c.id, name: c.name, modes: c.modes, variables: vars });\n    }\n  }\n  const chunk = {\n    done,\n    cursor: done ? null : encodeCursor(nextOffset),\n    hash: null,\n    collections: collectionsOut\n  };\n  if (offset === 0) {\n    chunk.textStyles = (await figma2.getLocalTextStylesAsync()).map(serializeStyle);\n    chunk.effectStyles = (await figma2.getLocalEffectStylesAsync()).map(serializeStyle);\n  }\n  if (done) {\n    const fullCollections = [];\n    for (const c of collectionsMeta) {\n      const variables = [];\n      for (const id of c.variableIds) variables.push(await serializeVariable(figma2, id));\n      fullCollections.push({ id: c.id, name: c.name, modes: c.modes, variables });\n    }\n    const textStyles = (await figma2.getLocalTextStylesAsync()).map(serializeStyle);\n    const effectStyles = (await figma2.getLocalEffectStylesAsync()).map(serializeStyle);\n    const full = { collections: fullCollections, textStyles, effectStyles };\n    chunk.hash = fnv1a64(stableStringify(full));\n  }\n  return chunk;\n}\n\n// src/figma/extract-entry.ts\n(async () => {\n  const args = JSON.parse(\"__ADHD_ARGS__\");\n  return runExtract(figma, args);\n})();\n"
}